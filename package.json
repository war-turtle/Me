{
  "name": "myresume",
  "version": "1.0.0",
  "description": "A resume made for showcasing my skills",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass public/scss -o public/styles && node index.js",
    "start": "nodemon -e ejs,scss,js,json --exec npm run build"
  },
  "author": "war-turtle",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.3",
    "node-sass": "^4.9.0"
  }
}
