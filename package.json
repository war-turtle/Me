{
  "name": "myresume",
  "version": "1.0.0",
  "description": "A resume made for showcasing my skills",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node-sass public/scss -o public/styles && node index.js",
    "dev": "nodemon -e ejs,scss,js,json --exec npm run build",
    "start": "node index.js"
  },
  "author": "war-turtle",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "node-sass": "^4.11.0"
  }
}
